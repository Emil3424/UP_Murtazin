<Window x:Class="UP_Murtazin.Okna.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мой профиль" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Border Style="{StaticResource CardStyle}" Padding="20">
            <StackPanel>
                <!-- Заголовок -->
                <TextBlock Text="Мой профиль" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryTextBrush}" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Аватар -->
                <Border Width="120" Height="120" CornerRadius="60" Background="#3498DB" Margin="0,0,10,0">
                    <Image x:Name="UserAvatar" Stretch="UniformToFill" Height="120" Width="120">
                    </Image>
                </Border>

                <!-- Информация о пользователе -->
                <Grid Margin="0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="ФИО:" 
                               Foreground="{StaticResource SecondaryTextBrush}" 
                               FontWeight="SemiBold" Margin="0,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="FullNameText" 
                               Foreground="{StaticResource PrimaryTextBrush}" Margin="0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" 
                               Foreground="{StaticResource SecondaryTextBrush}" 
                               FontWeight="SemiBold" Margin="0,10"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="EmailText" 
                               Foreground="{StaticResource PrimaryTextBrush}" Margin="0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Роль:" 
                               Foreground="{StaticResource SecondaryTextBrush}" 
                               FontWeight="SemiBold" Margin="0,10"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="RoleText" 
                               Foreground="{StaticResource PrimaryTextBrush}" Margin="0,10"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Телефон:" 
                               Foreground="{StaticResource SecondaryTextBrush}" 
                               FontWeight="SemiBold" Margin="0,10"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="PhoneText" 
                               Foreground="{StaticResource PrimaryTextBrush}" Margin="0,10"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Должность:" 
                               Foreground="{StaticResource SecondaryTextBrush}" 
                               FontWeight="SemiBold" Margin="0,10"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="PositionText" 
                               Foreground="{StaticResource PrimaryTextBrush}" Margin="0,10"/>
                </Grid>

                <!-- Кнопка закрытия -->
                <Button Content="Закрыть" Style="{StaticResource PrimaryButtonStyle}"
                        Width="120" Height="35" HorizontalAlignment="Center" 
                        Margin="0,20,0,0" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>